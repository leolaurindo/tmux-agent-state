# Source this file after the theme in ~/.tmux.conf.
set -g @agent_accent "colour39"
set -g @agent_highlight_when "running"

# Keep the theme's format and insert color only before its window-name token.
run-shell -b 'bash "#{q:#{d:current_file}}/agent-idle.sh"'
