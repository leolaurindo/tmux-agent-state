# Source this file after the theme in ~/.tmux.conf.
set -g @agent_highlight_when "running"
set -g @agent_highlight_style "underscore"

# Keep the theme's format and insert the highlight style before its window name.
run-shell -b 'bash "#{q:#{d:current_file}}/agent-status.sh"'
