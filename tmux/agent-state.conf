# Source this file after the theme in ~/.tmux.conf.
# Configure optional styles in ~/.tmux.conf after the source-file line.
# Keep the theme's format and insert agent styles before the window name.
run-shell -b 'bash "#{q:#{d:current_file}}/agent-state.sh"'
